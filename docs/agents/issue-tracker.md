# Issue Tracker Configuration

Issues and specifications in this repository live as local markdown files under `.scratch/`.

- **Active Specs**: `.scratch/specs/*.md`
- **Active Tickets**: `.scratch/tickets/*.md`

Each ticket is a self-contained markdown file declaring its title, description, and blocking edges (`blocked_by`).
